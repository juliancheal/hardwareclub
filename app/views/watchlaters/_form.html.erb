<%= form_for(@watchlater) do |f| %>
  <% if @watchlater.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@watchlater.errors.count, "error") %> prohibited this watchlater from being saved:</h2>

      <ul>
      <% @watchlater.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
